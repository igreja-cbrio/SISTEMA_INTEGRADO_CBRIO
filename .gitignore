# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backend/.env
.env
.env.*
!.env.example

# Playwright
e2e/.report/
e2e/.results/
playwright-report/
test-results/

# Tmp/scratch (added 2026-05-04)
tmp/
*.zip
guia-implementacao-cerebro-cbrio.md
.tmp/

# Skills locais (npx skills add) — tooling, não versionar
.agents/
skills-lock.json
.vercel

# Exports pro Google Stax (dado real · nunca commitar — só o exemplo sintético fica)
backend/scripts/stax-export/export_*.csv

# Grafo do graphify · derivado do código, ~26 MB, reconstruível em 3 min
# (o que É versionado é o .graphifyignore, que diz o que a ferramenta não lê)
graphify-out/

# Cache do `npm run typecheck` (tsc -b). Não versionar: é derivado e muda a cada
# execução, e um .tsbuildinfo desatualizado no repo faz o tsc PULAR arquivos —
# a checagem passaria sem checar, que é exatamente o buraco que ela existe para
# fechar.
*.tsbuildinfo
.env*
