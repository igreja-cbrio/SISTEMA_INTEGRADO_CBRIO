// Re-export the single Supabase client instance to avoid duplicates.
// All imports should use: import { supabase } from '@/supabaseClient';
// or from this path: import { supabase } from '@/integrations/supabase/client';
export { supabase } from '../../supabaseClient';